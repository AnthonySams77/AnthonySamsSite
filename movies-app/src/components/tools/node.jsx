import React, { Component } from "react";

class Node extends Component {
  state = {
    value,
    next
  };

  Node(value, next) {
    this.value = value;
    this.next = next;
  }

  render() {
    return <div></div>;
  }
}

export default Node;
